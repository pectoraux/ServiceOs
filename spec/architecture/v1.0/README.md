# ServiceOS Architecture v1.0

v1.0 is the frozen implementation baseline.

## Canonical documents

- `architecture.md` — architecture and ownership
- `architecture-lock.md` — immutable invariants
- `zeck-boundary.md` — AI ownership boundary
- `domain-model.md` — core domain objects
- `work-execution-model.md` — Service Work lifecycle and attempt model
- `vertical-model.md` — vertical package boundary
- `integration-model.md` — external provider boundary

The architecture may be changed only through an Architecture Change Request that creates a new immutable architecture version.
