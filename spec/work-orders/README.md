# Work Orders

Work Orders are the persistent implementation instructions for ServiceOS.

Identity rule: exactly one `WORK-NNN.md` per Work Order identity. A Work Order is `planned` until explicitly activated by the Architect.

Implementation rule: one activated Work Order maps to one implementation branch and one active PR.
